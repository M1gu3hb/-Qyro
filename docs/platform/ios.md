# iOS

Objetivo: Document/Photo Picker, Local Network/Bonjour, MultipeerConnectivity, AVFoundation, Keychain y file protection. Share Extension después del núcleo. iOS puede suspender la aplicación; no prometer background indefinido.

Estado: runner y código nativo no implementados; requiere macOS para build.
